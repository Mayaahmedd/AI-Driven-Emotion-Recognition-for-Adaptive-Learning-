"""adaptive_tutor.utils — small reusable utilities (seeding, hashing, etc.).

Nothing in this package may import from ``adaptive_tutor.core`` or any other
sub-package — it sits at the very bottom of the dependency DAG.
"""
